services:
  thermal-printer-service:
    build:
      context: .
      dockerfile: apps/thermal-printer-service/Dockerfile
    environment:
      PORT: ${SERVICE_PORT:?SERVICE_PORT must be set in .env}
      PRINTER_TRANSPORT: ${PRINTER_TRANSPORT:-lan}
      PRINTER_HOST: ${PRINTER_HOST:-192.168.1.100}
      PRINTER_PORT: ${PRINTER_PORT:-9100}
      PRINTER_TIMEOUT_MS: ${PRINTER_TIMEOUT_MS:-5000}
      PRINTER_USB_VENDOR_ID: ${PRINTER_USB_VENDOR_ID:-}
      PRINTER_USB_PRODUCT_ID: ${PRINTER_USB_PRODUCT_ID:-}
      CLIENT_ORIGIN: http://localhost:${CLIENT_PORT:?CLIENT_PORT must be set in .env}
    ports:
      - '${SERVICE_PORT}:${SERVICE_PORT}'
    healthcheck:
      test:
        [
          'CMD',
          'node',
          '-e',
          "fetch('http://localhost:${SERVICE_PORT}/api').then(r=>{if(!r.ok)process.exit(1)}).catch(()=>process.exit(1))",
        ]
      interval: 5s
      timeout: 3s
      retries: 10
      start_period: 10s
    restart: unless-stopped

  thermal-printer-simple-client:
    build:
      context: .
      dockerfile: apps/thermal-printer-simple-client/Dockerfile
    depends_on:
      thermal-printer-service:
        condition: service_healthy
    environment:
      CLIENT_PORT: ${CLIENT_PORT:?CLIENT_PORT must be set in .env}
      SERVICE_PORT: ${SERVICE_PORT:?SERVICE_PORT must be set in .env}
    ports:
      - '${CLIENT_PORT}:${CLIENT_PORT}'
    healthcheck:
      test:
        [
          'CMD-SHELL',
          'wget --quiet --tries=1 --spider http://127.0.0.1:$${CLIENT_PORT}/',
        ]
      interval: 5s
      timeout: 3s
      retries: 5
      start_period: 5s
    restart: unless-stopped
