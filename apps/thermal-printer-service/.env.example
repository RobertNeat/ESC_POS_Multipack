PORT=3000
PRINTER_TRANSPORT=lan
PRINTER_HOST=192.168.1.100
PRINTER_PORT=9100
PRINTER_TIMEOUT_MS=5000

# For PRINTER_TRANSPORT=usb, omit both IDs to auto-detect a printer-class
# device or set both values (decimal or 0x-prefixed hexadecimal).
# PRINTER_USB_VENDOR_ID=0x0416
# PRINTER_USB_PRODUCT_ID=0x5011
